public class Solution {
    public int JumpFloor(int target) {
        if(target < 0){
            return 0;//必须加 防止出现负数情况
        }
        if(target < 2){
           return 1;
        }else{
           return JumpFloor(target - 1) + JumpFloor(target - 2);
        }
    }
}