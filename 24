斐波那契数列类型问题


class Solution {
public:
     int jumpFloor(int n) {
        if(n <= 0)  return 0;
        else if(n == 1)  return 1;
        else if(n == 2)   return 2;
        else
            return (jumpFloor(n-1) + jumpFloor(n-2));
    }
};
